{
  "name": "radar",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/**/*.test.ts",
    "wohnraum:once": "tsx src/runWohnraum.ts --once",
    "wohnraum:loop": "tsx src/runWohnraum.ts",
    "alerts:once": "tsx src/runAlerts.ts --once",
    "alerts:loop": "tsx src/runAlerts.ts"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
